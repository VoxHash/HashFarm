# GitHub topics (discoverability)

Add these topics in the repository **About** settings on [github.com/VoxHash-Labs/HashFarm](https://github.com/VoxHash-Labs/HashFarm).

## Primary

`monero`, `xmr`, `p2pool`, `xmrig`, `randomx`, `cryptocurrency`, `mining`, `stratum`, `self-hosted`

## Stack

`python`, `fastapi`, `httpx`, `uvicorn`, `bash`, `linux`, `garuda-linux`, `macos`, `apple-silicon`

## Ops

`systemd`, `home-server`, `observability`, `open-source`

## Copy-paste (GitHub limit: 20 topics)

GitHub allows up to **20** topics. Suggested set:

```
monero,xmr,p2pool,xmrig,randomx,mining,stratum,python,fastapi,bash,linux,macos,self-hosted,open-source,cryptocurrency,httpx,uvicorn,home-server,systemd,observability
```

## Related repositories (not topics)

Link from the README or Wiki to official upstreams: Monero, P2Pool (SChernykh), XMRig.
